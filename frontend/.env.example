# Frontend environment — copy this file to .env and fill in your values

# Base URL of the backend API (no trailing slash)
VITE_API_URL=http://localhost:5000

# Google OAuth client ID (same value as the backend). Empty = Google login hidden.
VITE_GOOGLE_CLIENT_ID=
